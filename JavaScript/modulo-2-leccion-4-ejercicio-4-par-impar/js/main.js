'use strict';

/* remainder operator (%) to obtain the remainder of the division between two values */

function even(a) {
  if (a % 2 == 0) {
    return true;
  } else {
    return false;
  }
}

console.log(even(20));
