const sum = function (a, b) {
  return a + b;
};

console.log(sum(2, 3));

const restar = function (a, b) {
  return a - b;
};

console.log(restar(4, 3));

module.exports = {
  sum,
  restar,
};
