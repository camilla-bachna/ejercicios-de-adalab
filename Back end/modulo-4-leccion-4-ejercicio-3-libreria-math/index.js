const math = require("./math.js");
// que equivale a: import Header from './Header.js';

const a = 2;
const b = 3;

const resultadoSuma = math.sum(a, b);
console.log(resultadoSuma);
math.restar(a, b);
